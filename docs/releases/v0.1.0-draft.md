# Apollo Code v0.1.0 draft release notes

Status: **blocked; not published**.

The current L1 foundation includes a typed runner/provider/tool pipeline,
permission-aware tools, session storage, CLI integration, and native sandbox
workers. This draft is not a release declaration. See `L1-READINESS.md` for the
independent audit and remaining gates.

## Sandbox evidence

| Target | Native build | Escape execution | Tier | `escape.pass_ratio` |
| --- | --- | --- | --- | --- |
| `aarch64-apple-darwin` | Passed in CI | Passed current fixture in CI | Not recorded by CI; local macOS arm64 probe was Partial | Current fixture: 1/1; upstream suite not run |
| `x86_64-apple-darwin` | Passed in CI | Passed current fixture in CI | Not recorded | Current fixture: 1/1; upstream suite not run |
| `x86_64-unknown-linux-gnu` | Passed in CI | Passed current fixture in CI | Not recorded | Current fixture: 1/1; upstream suite not run |
| `aarch64-unknown-linux-gnu` | Cross-build only (`partial-verified`) | Not executed | Unverified | Unverified |

The table deliberately does not infer a Sandbox Tier from compilation or a
single escape fixture. Stable release remains blocked until the required suite
and doctor evidence exist for every L1 target.

## Remaining release gates

- Complete APO-12, APO-13, and APO-14.
- Re-run the complete L1 checklist on the resulting `main` revision.
- Obtain BDFL and security sign-off.
- Publish npm packages and create a tag only after explicit authorization.

