---
"apollo-code": minor
---

Replace the disconnected CLI shell with production L1 wiring for Runner, Anthropic, permissions, native sandbox workers, session JSONL resume, telemetry, auth, config health, and strict doctor checks.
