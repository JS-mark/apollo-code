---
'@apollo-code/core': minor
'@apollo-code/router': minor
---

Add priority fallback routing with retry budgets, per-provider cooldown and half-open probes, sticky-provider enforcement, and cancellation-aware backoff.
