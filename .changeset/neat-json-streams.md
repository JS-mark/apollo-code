---
'apollo-code': minor
'@apollo-code/core': minor
'@apollo-code/docs': patch
---

Add the versioned NDJSON contract for machine-readable chat output while preserving single-document JSON for management commands.
